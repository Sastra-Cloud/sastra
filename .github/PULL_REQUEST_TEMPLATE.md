## What changed and why

## How it was verified

- [ ] `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`
- [ ] Browser check for UI changes (light and dark, desktop and mobile)

## Checklist

- [ ] Help docs in `content/help/` updated for user-facing changes
- [ ] Migration, snapshot, and journal committed together for schema changes
- [ ] New environment variables added to `.env.example` and `docs/self-hosting/`
- [ ] `pnpm ai:map` run after route, symbol, or schema changes
